
// Interface representing a Workspace
export interface IWorkspace {
    id?: string;        // Unique identifier for the workspace
    link: string;      // The URL or path to the workspace
    name: string;      // The name of the workspace
}